<launch>
  <arg name="launch_driver" default="true"/>
  <arg name="vehicle_id" default="$(env VEHICLE_ID default)"/>
  <arg name="pointcloud_container_name" default="pointcloud_container"/>

  <group>
    <push-ros-namespace namespace="lidar"/>

    <!-- Topic remapping from CARLA to Autoware sensor topics -->
    <group>
      <push-ros-namespace namespace="top"/>
      <node pkg="topic_tools" exec="relay" name="lidar_top_relay">
        <param name="input_topic" value="/carla/$(var vehicle_id)/lidar_top"/>
        <param name="output_topic" value="pointcloud_raw"/>
      </node>
    </group>

    <!-- Pointcloud preprocessing for concatenation and filtering -->
    <include file="$(find-pkg-share carla_sensor_kit_launch)/launch/pointcloud_preprocessor.launch.py">
      <arg name="base_frame" value="base_link"/>
      <arg name="use_intra_process" value="true"/>
      <arg name="use_multithread" value="true"/>
      <arg name="use_concat_filter" value="true"/>
      <arg name="pointcloud_container_name" value="$(var pointcloud_container_name)"/>
    </include>
  </group>
</launch>
